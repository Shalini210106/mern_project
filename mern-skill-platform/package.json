{
  "name": "mern-skill-platform",
  "version": "1.0.0",
  "description": "Faculty Guided Student Skill Enhancement Platform",
  "scripts": {
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install-all": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
